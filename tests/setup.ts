// Add any global test setup here
// For example, if you need to mock certain browser APIs that jsdom doesn't provide

beforeAll(() => {
  // Add any one-time setup before all tests
});

afterAll(() => {
  // Add any one-time cleanup after all tests
});
